# LightGBM native models are integrity-checked byte for byte by manifest.json.
# Never let Git rewrite their line endings on checkout.
# Windows LightGBM may append a CRLF footer; retain and recognize those bytes.
artifacts/**/model.txt -text whitespace=cr-at-eol
