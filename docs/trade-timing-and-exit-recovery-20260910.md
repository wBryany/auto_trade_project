# 2026-09-10 main 交易时机与限频后的平仓恢复

## 数据与订单

对象为 main、8787、Binance production、BTCUSDT；时间均为北京时间。09-10 14:16 冻结了公开成交价 1m、5m、1h K 线：只增量发送 4 次公共请求，没有使用私有交易接口；三个周期均无缺口、无重复。用每笔入场时已经收盘的最多 299 根 K 线重建，四笔订单的方向、分数、信号时间戳和理由全部与实盘报告一致。

| 开仓 → 平仓 | 方向 / 数量 BTC | 成交价 | 毛盈亏 USDT | 报告全成本净盈亏 USDT |
| --- | --- | --- | ---: | ---: |
| 01:02 → 03:13 | 空 / 0.001 | 78698.8 → 78429.0 | +0.2698 | +0.15981054 |
| 06:03 → 06:10 | 空 / 0.001 | 78060.3 → 77894.4 | +0.1659 | +0.05673171 |
| 08:43 → 09:05 | 空 / 0.001 | 78137.3 → 78366.4 | -0.2291 | -0.33865259 |
| 09:50 → 10:52 | 空 / 0.001 | 78058.3 → 78292.4 | -0.2341 | -0.34354549 |

合计 4 笔、2 胜 2 负。成交差毛盈亏 -0.0275；报告手续费 0.31296845；另计模型滑点 0.12518738。报告全成本净值为 **-0.46565583 USDT**；按报告成交差仅减手续费为 **-0.34046845 USDT**。全成本净值包含额外滑点估计，不能称为交易所账单净值。

08:43 的 1m 反转空入场距此前 60 分钟最低点仅 6.5 美元，1m EMA9 乖离为 1.479 ATR，存在追空风险。当时 5m MACD 柱已经减弱、1h 方向仍偏空，已有反向动能过滤正常通过，不能归因为它没有生效。反转入口使用的是相对 5m EMA9 的距离，通用 1m 执行质量限制没有覆盖该入口。

09:50 是传统 `5m_pullback_reclaim` 空单：09:40 刚出现约 3.074 倍量的反弹，入场前 1m 量比只有 0.241；5m MACD 柱虽为负，却从 -25.365 回升至 -24.363。这里值得验证的是回落是否获得新的空头动能确认。入场距已知低点约 146.1 美元，不能简单称为恰好在最低价追空。

后两笔在线最有利价格变动仅约 73.71 / 106.8 美元，均不足覆盖当时模型约 109 美元的往返成本距离。两笔均由动态止损退出，没有触发时间退出。关闭止损、事后等待回落，不能作为安全的策略优化。

用户截图中的部分极值与本次 Binance BTCUSDT 数据不一致。例如 09:50—10:45 的最低成交价为 77951.5，截图为 77888；需要确认截图行情源/时间标识。实盘软止损与在线浮盈使用标记价格，成交价 K 线极值也不能直接替代标记价格。

## 11:26 告警及 10:49 的退出中断

这次是新的真实限频，并非此前已修复的旧 keepalive 错误重复告警。

| 时间 | 实际网络请求 | 结果 / 恢复 |
| --- | --- | --- |
| 10:49:39.280 | GET `/fapi/v1/time` | HTTP 418；截止 10:49:59.242；当时一分钟本服务仅 1 次 REST 请求 |
| 11:26:10.302 | PUT `/fapi/v1/listenKey` | HTTP 418；截止 11:35:59.437；一分钟本服务仅 1 次 REST 请求 |
| 11:35:59.764 | listenKey 续期 | HTTP 200；此后每半小时续期均成功 |

截图 11:26:18 是页面检测后告警入队时间，发送时间为 11:26:19.367。两次实际限频分别发送一封；至 14:21 共 91 次本服务 REST 请求。另一个本机 8788 服务使用 OKX paper，不是第二个 Binance 轮询进程。上述证据不能证明是谁耗尽出口额度，也不支持把事件归因为 main 每分钟刷满 2400 次请求。

行情与账户更新已经使用 WebSocket，listenKey 续期仍每半小时使用 REST。Binance 额度按出口 IP 计算；官方 WebSocket API 的 `userDataStream.ping` 也消耗 IP 请求权重。将续期从 REST 换成 WebSocket，不能保证共享出口不受限频影响。

- [Binance general information / rate limits](https://developers.binance.com/en/docs/products/derivatives-trading-usds-futures/general-info)
- [Binance WebSocket API user data streams](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/ws-api/user-data-streams)

10:49 的校时请求发生于主动平仓提交路径；当时没有 POST `/order` 发出。限频约 20 秒后评估恢复，但最终平仓请求直到 10:52:05 才发出。旧代码不保存已触发而未发出的退出意图，下一周期会重新判断条件，价格暂时回落便可能继续持仓。原日志没有首次退出决策价/原因，不能把约 147 秒全部算作封禁时长，也不能精确计算因此产生的额外损失。

## 修复范围与验证

退出恢复修改使用结构化的“目标请求未发送”标记。仅对本地限频门控、或签名前校时失败所阻断的平仓请求保存退出原因、首个决策价、仓位身份及重试时间。解封后先重新核对同一仓位，再执行原先的 reduce-only 退出；如果交易所已经平仓或仓位改变，清除旧意图。首次决策与提交前均留下可审计日志。

待退出状态保存在现有托管仓位文件中，可随服务重启恢复。重试提交前必须成功落盘清除“未发送”资格，防止请求结果不确定或进程崩溃后复用旧的自动重试资格。真实订单 HTTP 拒绝、网络超时、提交后的成交查询失败不会被标记为未发送；此限定不代表原有正常止损条件日后再次触发时也禁止退出。校时收到 418/429 时保留原始拒绝原因与期限，不再用旧时钟锚点吞掉该错误。同一开仓订单的晚到成交明细修正时间/均价时，同步迁移待退出记录的仓位身份，避免把元数据补记误当成换仓。

初步连续候选比较如下；起始资金为模拟 10000 USDT、风险仓位随权益计算，金额不是实际账户损益。所有候选均不启用持仓时间限制。

| 09-02—09-10 连续回放 | 笔数 / 赢单 | 模拟全成本净盈亏 | 模拟最大回撤 |
| --- | ---: | ---: | ---: |
| 当前策略 | 24 / 12 | -734.98 | 8.232% |
| 反转相对 5m EMA9 距离上限 1.25 → 1.0 ATR | 23 / 12 | -666.90 | 7.898% |
| 反转增加通用 1m 执行质量检查，1.5 ATR | 24 / 12 | -734.98 | 8.232% |
| 反转增加通用 1m 执行质量检查，1.0 ATR | 23 / 12 | -686.92 | 7.810% |
| 仅传统回抽空单要求 5m MACD 柱继续下降 | 24 / 12 | -664.83 | 7.810% |

09-02—09-07 四组结果完全相同；1m 1.0 ATR 到 09-09 仍与基线相同；5m 1.0 ATR 在 09-08 反而变差约 9.11 USDT。表面改善主要来自今天一个坏单，1m 1.0 ATR 还会挡掉 01:02 的盈利单并产生其他替代入场。这不构成稳健优化证据，因此不部署这些入场参数。09-08—09-10 是看过订单后的诊断区间，不称为独立样本外验证。

传统回抽空单候选的改进也全部集中在今天：把 09:50 的传统空换成 10:05 的超短空，模拟仓位约减半；新入场更低，反向价格距离由 232 美元增至 247.2 美元。23 笔共有交易完全一致。按实盘 0.001 BTC 同等数量重新计算，该替换的模型净盈亏反而从 -0.341451 变为 -0.356556。这是仓位变化带来的表面改善，不是已证明的入场时机改善，因此也不部署。

本轮只部署已证实的退出恢复与错误来源修复；策略入场参数、仓位配置及关闭时间退出的设置保持原值。

验证：完整 pytest 539 项通过；另在生产 Python 3.12、原生 websocket-client 1.9.1 下运行退出恢复、HTTP/签名请求身份、私有流恢复和启动测试，共 68 项通过。覆盖真实校时 HTTP 418 后目标订单未发送、解封时价格已回落、重复限频、同仓位重启、迟到成交补记、外部先平仓、仓位不匹配、未知订单结果及原子落盘失败。独立复审通过，`git diff --check` 无错误。

部署：09-10 14:30 使用 `scripts/restart_bot.ps1` 重启 main 8787，独立进程 PID 48988。重启前、停止后、启动前和启动后均确认空仓、无挂单。14:31 再次检查运行中、首个行情周期已完成、行情和私有 WebSocket 健康、无最新错误、无重试期限；时间退出仍关闭。运行日志为 `logs/dashboard.restart.20260910-143036.stderr.log`。本次不停止或修改 8788 的 OKX paper 服务。

本次离线诊断文件位于 `logs/trade-review-20260910/`（不入库）：`config-safe.json`、`data-summary.json`、`entry-reconstruction-audit.json`、`morning-orders.png` 和候选连续回放。仅保存非凭据配置。回放使用闭合 K 线和下一根开盘成交；不包含逐秒标记价格、真实盘口、网络中断和实盘最小下单量回退。现有反向信号退出分支仍以下一开盘价检查盈利，候选比较共享这一局限，结果不能视为已验证的真实收益或 80% 胜率承诺。
