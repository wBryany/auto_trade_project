# 8787 主分支交易复盘：2026-09-05

## 数据与结论

分析使用运行实例实际写入的 `C:/my_project/reports/binance_testnet/binance-production/trade_report.csv`，共 52 笔已平仓交易；主分支运行目录下的同名 CSV 为空，未将它误作实盘样本。净收益为应用按照配置计算的费用后收益，未重新审计每笔交易所手续费账单。

K 线来自 Binance USD-M BTCUSDT 公共 REST，复用本地历史数据并补齐至本次分析时刻。只用入场前已经收盘的 1m K 线计算过滤条件。原始数据及回放输出保存在运行目录 `logs/strategy-review-20260905/`，不提交行情大文件。

52 笔净收益合计 -7.67690 USDT，费用合计 5.72870 USDT；其中自 9 月 3 日 UTC 起的最近 15 笔净收益 -3.31361 USDT，费用 1.66961 USDT。最近几笔入场前一小时振幅只有约 0.16%–0.28%，当前双边手续费加滑点已需 0.14%，最低净收益要求另为 0.15%。固定止盈价提供的理论收益距离，并不代表实际市场有相应的波动空间。

90 分钟硬退出符合当前配置（软退出 3600 秒、硬退出 5400 秒），没有发现此次重启导致持仓时间参数丢失的证据。本次不缩短退出时间，也不放宽信号、仓位或止损。

## 修改

增加 `risk.entry_range_lookback_minutes`，类默认值为 0（关闭），主分支配置启用 60。入场时最近 60 根连续、已收盘 1m K 线的最高价减最低价，除以入场参考价，必须至少覆盖配置的预期往返费用与最低净收益之和；当前为 0.29%。资金费按照现有费用模型计入。

数据不足、时间缺口、无效价格时拒绝新入场。检查位于持仓管理之后、开仓私有 API 调用之前；不会阻止保护止损、主动平仓或原有反向信号退出。低振幅信号返回 `insufficient_market_range`，不反复尝试开仓。回测使用相同函数及决策时已知收盘价格。

这是波动与成本过滤，历史振幅不是预测收益，也不能保证能捕获整个区间。

## 验证与限制

逐笔筛查最近 15 笔会拦下 5 笔，合计净收益 -0.83533 USDT，其中 1 笔微利；更早 37 笔会拦下一笔 +0.27798 USDT 的盈利交易。全部 52 笔中拦下 6 笔，合计 -0.55735 USDT。此筛查不代表可实现的收益改善，因为它没有模拟后续交易顺序变化。

另从 2026-09-02 00:00 UTC 到本次数据终点进行连续 1m 回放，保留较早的 5m/1h 数据作为指标预热。使用当前有效策略与费用、模拟初始资金 10,000 USDT：

| 方案 | 平仓笔数 | 盈利笔数 | 期末资金 | 最大回撤 |
|---|---:|---:|---:|---:|
| 原规则 | 21 | 8 | 9745.03543 | 2.84440% |
| 60 分钟振幅过滤 | 15 | 6 | 9796.72942 | 2.30299% |

两个方案仍亏损。这是用于发现问题的同一近期样本，不能作为独立样本外盈利验证；历史版本的实盘入场、实际成交与模拟仓位也不同，回放金额不能直接折算为实际账户收益。阈值沿用既有费用和最低净收益要求，没有扫描参数选取最高收益点。

验证覆盖成本门槛、关闭过滤器、数据缺口与无效值、排除未收盘大波动 K 线、开仓前拦截私有调用、同一信号不重复尝试以及现有持仓退出优先。修改后全套原有及新增测试通过，持仓退出补充测试单独通过。
