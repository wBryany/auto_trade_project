# 2026-09-13 main 候选信号与开仓诊断

用户主动发起复盘。main / 8787 / Binance production BTCUSDT 在 09-12 14:06:35 的上次研究截止后没有新增完整平仓交易；最新仍为 09-12 09:09:19 的 `d64fcca779f34efa9c05d4ac5345c1d7`。服务持续运行、行情与私有 WebSocket 健康、空仓且无挂单。时间退出保持关闭，没有恢复自动巡检。

## 为什么没有新交易

核查 09-12 14:06:35—09-13 01:47:06 的运行日志，没有入场、退出、ERROR、418/429、私有接口失败或宏观入场熔断记录。两次宏观日历 403 均使用缓存回退。40,734 条 flat 日志之外，存在两个约一分钟的六分做空候选窗口：09-12 21:43:52—21:44:52、09-13 00:57:53—00:58:52。117 条 short 日志是每秒轮询重复，不是 117 次独立机会。

现有信号日志在后续风险、振幅和成本检查之前写入；同一候选通常只检查一次，随后返回 no_action。旧日志没有保存后续门槛返回结果或精确行情快照，因此“看到做空信号但没有订单”不能直接归类为漏单，也不能说完全没有信号。

为核查门槛，共进行了 **2 次受控公共行情请求**，每次最多尝试一次，只读取两个历史时段的 1m K 线。按候选日志所在分钟及相邻已闭合窗口复算，60 分钟价格区间均不足门槛：

| 候选日志时间 | 同分钟之前的闭合 60 根区间 | 参考收盘价 | 振幅 | 要求振幅 |
| --- | ---: | ---: | ---: | ---: |
| 09-12 21:43 | 156.5 美元 | 77264.8 | 0.20255% | 0.29%（约 224.07 美元） |
| 09-13 00:57 | 112.3 美元 | 77324.8 | 0.14523% | 0.29%（约 224.24 美元） |

前后相邻窗口也全部不通过。0.29% 来自当前配置的往返手续费与滑点估计 0.14%，加最低净空间 0.15%。这是观察到的波动范围过滤，不是未来利润预测；即使达到门槛也不能保证能赚到整个区间。正常空仓评估使用闭合 1m 收盘价；旧日志未保存准确的被评估 K 线快照，因此该复算证明未开仓与振幅门槛高度一致，不冒充当时的精确返回状态。

连续亏损阈值关闭、此前冷却已结束；当前会话已平仓损益也远未达到其配置的会话亏损门槛。结合无异常与无宏观阻断的日志，未发现本轮需要放宽风险或成本门槛的依据。

## 本轮修改范围

补齐开仓被拒绝的诊断日志：风险暂停、实际振幅不足、预计收益不覆盖成本时，记录信号时间、方向、分数、具体原因与必要数值。振幅判定保留原布尔接口并共用同一评估逻辑，缺失或无效数据仍然拒绝开仓。保留已有振幅拒绝返回字段；同一信号不会因每秒轮询重复写拒绝日志，不新增交易所请求或邮件。

不改变入场信号、保护门槛、止损边界、仓位预算、冷却或时间退出设置。此次是消除复盘盲点，不能将它宣称为已经提升交易胜率。没有新增已完成交易，本轮不重复运行上次的参数搜索；此前提前保本及小时动能过滤均维持未部署状态。

验证：完整 pytest **571 项通过**，针对拒绝诊断、振幅、引擎及对账恢复的测试集 74 项通过。新增测试覆盖相等与略低边界、无效或缺失数据、持仓退出优先、拒绝后不读取权益/不下单、两阶段成本检查、每秒重复轮询去重及私有重试状态保留。独立复核的 450 组旧/新振幅判定结果一致；原会话风险谓词保持不变。部署沿用 `scripts/restart_bot.ps1` 的独立 Windows 进程启动与前后仓位、订单、连接及首周期检查。

证据：`logs/strategy-research/no-new-trades-20260913.json`、`logs/strategy-research/status-check-20260913.json`、`logs/trade-review-20260913/candidate-range-check.json`。日志与原始行情不入库。
