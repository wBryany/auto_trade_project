# 反转确认与取消时间退出：2026-09-09

## 生效规则

main / 8787 根据用户明确要求，关闭 `enable_time_exit`。持仓不再因达到 60 分钟软上限或 90 分钟硬上限而退出；原阈值仅作为未启用的比较参数保留。交易所硬止损、本地动态止损、保本及追踪保护、突破失败、趋势失效及其他原有价格/信号退出仍有效。页面显示实际运行中的时间退出状态，避免保存配置但尚未重启时误示已生效。

超短反转的 5m 动能过滤继续开启，将 `traditional_ultra_short_reversal_max_adverse_macd_atr` 从 0.05 收紧为 0。反转做空时，若闭合 5m 的正向 MACD 柱还在增强，则拒绝；反转做多采用镜像规则。持平或减弱允许继续检查原有 1m、量能和空间条件，不必等待 5m MACD 翻零。复用现有过滤实现，覆盖普通超短反转与 pullback reversal。

已同步 main 的 Binance 配置、示例配置及本机实际使用的 local 配置。此次不是所有入口的全局顺势限制：独立 predictive reversal 有自己的尖峰失败、1m 均线跌破及 MACD 转负条件，本次未修改该路径。

## 本次订单与复算

北京时间 14:35:02 多单以 79161.1 入场，16:01:53 以 79273.2 触发 `time_exit`；86.85 分钟持仓达到时间门槛且满足程序扣成本盈利条件。报表净收益 +0.001196 USDT，包含滑点估算。约 13 分钟后，16:15:00 的独立超短反转空单以 79377.9 入场，16:28:05 以 79635.0 触发 `dynamic_stop_loss`，报表净亏 0.368409 USDT。这笔空单平仓与时间上限无关。

16:15 触发时，闭合 1h MACD 柱为 +101.63，较前值 +92.18 增强；5m 柱从 +13.116 增至 +13.526，RSI 64.40 满足旧规则的高 RSI 反转背景，0.410 的动能增量又低于原 0.05 ATR 容忍值 4.915。1m 16:14 阴线从 79446.6 收至 79377.9，跌破前低，导致旧版提前开空。

使用当时每周期 299 根闭合 K 线重建：严格过滤将 16:15 的 short 改为 flat，14:35 的 long 信号及全部理由保持一致。独立 predictive reversal 在旧、新配置下均未确认本次入场。

## 连续回放

使用本机 `logs/trade-review-20260909-1615/` 冻结的 1m/5m/1h 数据，执行 `deploy_replay.py` 的 before、strict_guard、no_time_strict 三组，重新生成全部入场、退出、冷却和持仓路径。1m 覆盖 9/2 08:00 至 9/9 16:30，5m/1h 含更早预热；模拟初始资金 10000，费用及风险参数相同。

| 方案 | 平仓 / 盈利笔数 | 净胜率 | 模拟净收益 | 最大回撤 | 平均持仓 |
|---|---:|---:|---:|---:|---:|
| 修改前 | 26 / 12 | 46.15% | -838.49 | 9.38% | 39.35 分钟 |
| 仅收紧反转过滤 | 20 / 11 | 55.00% | -581.65 | 6.78% | 40.55 分钟 |
| 收紧过滤并取消时间退出（本次采用） | 19 / 9 | 47.37% | -685.32 | 7.81% | 77.16 分钟 |

过滤改善了两段诊断样本：9/8 以前净收益从 -407.98 至 -352.00，9/8 以来从 -430.51 至 -229.66。取消时间退出后对应为 -344.56、-340.76。取消时间退出没有在此样本中独立改善收益，但按用户明确的持仓偏好启用；不能将其描述为已经验证的增益。

这些仍是亏损样本，不支持 80% 胜率。结果使用分钟 OHLC、估算费用/滑点与模拟仓位，不含真实盘口、最小订单回退及运行中断；模拟金额不能直接对应当前小额账户。既有 backtest 的 opposite_signal 盈利判断仍依赖下一根开盘价，是三组共同的已知局限；本次没有扩大修改范围到该模拟分支。数据末端未平仓不强制平仓。

## 回归与部署

新增 11 项反转确认测试和 10 项时间退出/保护测试，覆盖本次入场、镜像方向、允许动能减弱、继续要求 1m 确认、关闭时间退出后超过两小时仍可持仓，以及止损和趋势退出照常生效。测试不请求交易所或发送真实邮件。

全套 `pytest -q` 367 项通过；页面两个内联脚本通过 Node 语法检查。16:39 使用标准 `scripts/restart_bot.ps1 -ConfigPath config.binance.testnet.local.json` 独立启动服务，PID 39280；四次安全快照均为空仓、无挂单，首轮运行及行情/账户健康检查通过。16:40 的实际 API 返回 running=true、last_error 为空、time_exit.enabled=false、soft_seconds/hard_seconds 均为 null；页面 HTTP 200，包含时间退出状态展示。本机有效策略与修复前比较，仅上述两个设置改变。部署日志：`logs/dashboard.restart.20260909-163902.stderr.log`。
